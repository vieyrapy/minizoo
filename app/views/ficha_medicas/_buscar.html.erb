    <div class="navbar-form ">
           <div class="form-group">
                <%= form_tag animales_path, :method => 'get' do %>
                        <%= text_field_tag :Buscar, params[:Buscar], :class=>"form-control", placeholder: "Nombre común"  %>
                    <%= submit_tag "Buscar" , :class=>"btn btn-default"%>
                <% end %>
              </div>
    </div> 
     <hr></hr>  