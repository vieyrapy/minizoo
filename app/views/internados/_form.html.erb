<%= form_with(model: internado, local: true) do |form| %>
  <% if internado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(internado.errors.count, "error") %> prohibited this internado from being saved:</h2>

      <ul>
      <% internado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :medicamento %>
    <%= form.text_field :medicamento, id: :internado_medicamento %>
  </div>

  <div class="field">
    <%= form.label :dosis %>
    <%= form.text_field :dosis, id: :internado_dosis %>
  </div>

  <div class="field">
    <%= form.label :fecha_inicio %>
    <%= form.datetime_select :fecha_inicio, id: :internado_fecha_inicio %>
  </div>

  <div class="field">
    <%= form.label :fecha_fin %>
    <%= form.datetime_select :fecha_fin, id: :internado_fecha_fin %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion, id: :internado_descripcion %>
  </div>

  <div class="field">
    <%= form.label :dar_alta %>
    <%= form.check_box :dar_alta, id: :internado_dar_alta %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
