

<!-- Alerta para un usuario vip -->
<% if current_user.try(:vip?) %> 
      <div class="alert alert-danger" role="alert">
      <h2>No tiene permiso para utilizar esta sección consulte con el administrador</h2>
<% end %>
<!-- fin de alerta -->

<!-- Si es admin y user se muestra la pagina -->
<% if user_signed_in? %>
  <% if current_user.try(:user?) || current_user.try(:admin?)  %> 
<!-- finaliza al pie de esta lamina con dos end  -->



  <h3>Zona de Animales con tratamientos</h3>
  <hr></hr>
  
  <div>
    <%= link_to 'Hoy', tareas_path, method: "get", class:"btn btn-primary" %>    
    <%= link_to 'Todos', tareas_path({terminado: 'false'}), method: "get", class:"btn btn-primary" %>
  </div>
       
<br>
<table class="table table-condensed">
  <thead>
    <tr>
      <th>FECHA/HORA</th>
      <th>ID</th>
      <th>NOMBRE COMÚN</th>
      <th>DIAGNÓSTICO</th>
      <th>MEDICAMENTO</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tareas.each do |tarea| %>
      <tr>
        <td><%= l(tarea.fecha, :format => :long) %></td>
        <td><%= tarea.ficha_medica.animal.identificador %></td>
        <td><%= tarea.ficha_medica.animal.nombre_comun %></td>
        <td><%= tarea.ficha_medica.dolencia %></td>
        <td><%= tarea.medicamento %></td>
        <!-- <td><%= check_box_tag (tarea.terminado) %></td>  -->

        <td><%= link_to 'Mostrar', tarea.ficha_medica, :class=>"btn btn-mostrar"  %></td>
        <td> <%= link_to 'Dar de Alta', daralta_tarea_path(tarea), method: "put", class:"btn btn-alta" %> </td>

        <!-- <td><%= link_to 'Show', tarea %></td>
        <td><%= link_to 'Edit', edit_tarea_path(tarea) %></td>
        <td><%= link_to 'Destroy', tarea, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>


<% end %>
<% end %>